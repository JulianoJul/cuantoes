name: cuantoes
description: "Tasas de cambio BCV: USD, EUR, USDT a VES"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  html: ^0.15.4
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  provider: ^6.1.2
  google_mlkit_text_recognition: ^0.17.1
  image_picker: ^1.2.3
  camera: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
